VITE_API_URL=http://192.168.219.128

# Privacy Mirror — STUN server for WebRTC ICE gathering (UDP port 3478)
VITE_STUN_URL=stun:192.168.219.128:3478

# Privacy Mirror — direct HTTPS for TLS fingerprint (bypasses nginx to preserve ClientHello)
# Open this URL in browser and accept the self-signed certificate before using the TLS tab
VITE_FINGERPRINT_URL=https://192.168.219.128:8443
